defmodule GmBmd do
  @moduledoc """
  GM Business Management Dashboard — THOR3 action.

  The domain lives under `GmBmd.Bridge` (data source), `GmBmd.Gm` (bridge
  maths), `GmBmd.Outturn`, `GmBmd.Daily`, `GmBmd.Rules`, `GmBmd.Revenue` and
  `GmBmd.Targets` (persisted T2 targets). Everything is transactions; revenue
  (AED) is derived and always secondary.
  """
end
